{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-back",
      "command": "dotnet",
      "type": "process",
      "args": ["build", "-c", "Debug", "${workspaceFolder}/back/back.csproj"],
      "options": {
        "cwd": "${workspaceFolder}/back"
      },
      "problemMatcher": "$msCompile",
      "group": "build"
    }
  ]
}
