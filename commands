instalar sdk net en ubuntu
sudo apt update
sudo apt install -y dotnet-sdk-8.0

consultar version net
dotnet --version

en ubuntu
Ctrl + Alt + T. 
dotnet --info

mantener back en caliente
dotnet watch run

setear credenciales get
git config --global user.name "jcalvo"
git config --global user.email "jonathan.nc3@gmail.com"

verificar credenciales git
git config --global user.name
git config --global user.email

limpiar credenciales git
git config --global --unset user.name
git config --global --unset user.email

comandos para ordenar codigo
shift + alt + f en front
ctrl + shift + i en back

dotnet build
dotnet run
dotnet clean
dotnet build -c Debug

npm outdated              # ver qué hay para subir
npm update                # sube a la última versión permitida por los rangos
rm -rf node_modules package-lock.json
npm install               # limpia y re-resuelve dependencias
npm dedupe

npx npm-check-updates@latest -u